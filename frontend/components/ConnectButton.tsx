import React from 'react';
import { WalletSelector } from './WalletSelector';

const ConnectButton = () => {
    return (
        <div>
            <WalletSelector />
        </div>
    );
};

export default ConnectButton;